{
  "scripts": {
    "prepare": "husky install",
    "g:storybook": "cd $INIT_CWD && storybook dev -p 6006"
  },
  "devDependencies": {
    "lerna": "^6.0.1",
    "sort-package-json": "^1.50.0"
  },
  "name": "js-sdk",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
