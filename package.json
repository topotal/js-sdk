{
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "lerna": "^6.0.1",
    "sort-package-json": "^1.50.0"
  },
  "name": "js-sdk",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
