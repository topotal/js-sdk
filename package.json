{
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "sort-package-json": "^1.50.0"
  },
  "name": "js-sdk",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
